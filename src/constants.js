export const loginEndpoint = "https://localhost:8443/login";

export const registerEndpoint = "https://localhost:8443/register";

export const emailRegex = new RegExp("[A-Za-z0-9._%-+]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,4}");

export const notificationType = Object.freeze({ success: "success", fail: "fail" });
